# Placeholder Nginx config — M9 will implement real reverse proxy and SPA routing.
events {}
http {
  server {
    listen 8080;
    location / {
      return 200 'Nginx placeholder — M9 will serve SPA and proxy API.';
      add_header Content-Type text/plain;
    }
  }
}
