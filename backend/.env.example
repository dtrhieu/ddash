# Backend environment variables (local defaults)
# Copy this file to backend/.env and adjust as needed

# Django
DEBUG=1
SECRET_KEY=change-me-in-prod

# Database (choose one of the two options below)
# Option A: Single URL
# DATABASE_URL=postgres://ddash:ddash@localhost:5432/ddash

# Option B: Individual Postgres variables
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ddash
POSTGRES_USER=ddash
POSTGRES_PASSWORD=ddash

# Redis for cache/Celery
REDIS_URL=redis://localhost:6379/0
