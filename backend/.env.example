# Backend environment variables (local defaults)
# Copy this file to backend/.env and adjust as needed

# Django
DEBUG=1
SECRET_KEY=change-me-in-prod

# Database (choose one of the two options below)
# Option A: Single URL
# DATABASE_URL=postgres://ddash:ddash@localhost:5432/ddash

# Option B: Individual Postgres variables
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ddash
POSTGRES_USER=ddash
POSTGRES_PASSWORD=ddash

# Redis for cache/Celery
REDIS_URL=redis://localhost:6379/0

# Optional: initial superuser for local dev
# If you run: python backend/manage.py createsuperuser --noinput
# these will be used by Django's createsuperuser management command.
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=admin

# Convenience: When using scripts/bootstrap_real_db.sh, export CREATE_SUPERUSER=1
# to let the loader create admin/admin automatically.
